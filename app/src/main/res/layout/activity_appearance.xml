<?xml version="1.0" encoding="utf-8"?><!--
  Root layout uses SlideMenu. First child is layout_menu for side menu.
-->
<com.cos407.cs407finalproject.SlideMenu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/slideMenu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.cos407.cs407finalproject.AppearanceActivity">

    <!-- Include the side menu layout -->
    <include layout="@layout/layout_menu" />

    <!-- Main content area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Button to open/close side menu -->
        <ImageView
            android:id="@+id/myProfilePhoto"
            android:layout_width="61dp"
            android:layout_height="52dp"
            android:layout_marginBottom="20dp"
            android:clickable="true"
            android:contentDescription="@string/profile_photo"
            android:focusable="true"
            android:src="@drawable/photo" />

        <Switch
            android:id="@+id/switchTheme"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="Dark Theme" />
    </LinearLayout>
</com.cos407.cs407finalproject.SlideMenu>
